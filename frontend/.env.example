# Local dev — FastAPI running on your machine
VITE_API_URL=http://localhost:8000

# Prod — set this in Vercel's project env vars to your Render backend URL, e.g.:
# VITE_API_URL=https://motionx-backend.onrender.com
